# Current Work

**Last updated**: 2026-03-12

## In Progress

No active work items.

## Next Steps

- Consolidate duplicated frontend query-string builders across `frontend/src/api/entities.ts`, `sources.ts`, `search.ts`, `explanations.ts`, `inferences.ts`, and `extractionReview.ts` into shared request-serialization helpers (`appendArrayParam`, `appendOptionalNumber`, `buildQueryString`). These modules repeat the same `URLSearchParams` control flow with different field names, so transport-format changes currently fan out across many files.
- Extract a shared frontend async-detail resource pattern for views like `SourceDetailView.tsx`, `EvidenceView.tsx`, `SynthesisView.tsx`, `DisagreementsView.tsx`, and `ExplanationView.tsx`. They repeat the same `loading/error/reset/parseError/showError/isMounted` lifecycle and should use a common controller hook or async resource utility instead of each page reimplementing it.
- Move repeated relation-refresh logic out of `frontend/src/views/SourceDetailView.tsx` into a dedicated hook/action helper. The view currently duplicates `listRelationsBySource -> setRelations -> setRelationsError -> parseError/showError` after initial load, relation deletion, and extraction save.
- Consolidate repeated frontend error-notification plumbing around `parseError(...)` plus `showError(...)` into a shared page-action helper. Many views and admin/import flows duplicate the same catch blocks, which makes message policy and logging inconsistent.
- Introduce a shared backend search execution pipeline for `backend/app/services/search_service.py`. `_search_entities`, `_search_sources`, and `_search_relations` each rebuild the same lifecycle: normalize query, compose base query, calculate relevance, count, paginate, execute, and map rows. Extract common search templates and keep only domain-specific predicates/mappers per result type.
- Extract reusable snippet/relevance policy helpers from `backend/app/services/search_service.py`. Each `_search_*` path defines its own inline `case(...)` relevance logic and ad-hoc snippet generation, duplicating search-policy code instead of centralizing ranking and summary truncation rules.
- Consolidate repeated extraction orchestration steps in `backend/app/api/document_extraction.py` into application services. The module repeats the same sequences for `SourceService` lookup, source validation, `BatchExtractionOrchestrator` invocation, `EntityLinkingService` matching, `BulkCreationService` save flow, and exception wrapping across document, URL, and PubMed paths.
- Remove duplicated validation/error wrapping in `backend/app/api/document_extraction.py` by introducing shared request validators and endpoint wrappers. The file repeats many near-identical `ValidationException` guards and `except (AppException, ValidationException)` blocks with the same fallback `AppException(...)` translation.
- Centralize repeated PubMed fallback/import mapping logic in `backend/app/api/document_extraction.py`. Test fallback article generation, PMID-to-article conversion, and trust-level/source payload mapping appear in multiple PubMed search/import paths and should live in a dedicated adapter module.
- Split duplicated frontend test setup into shared builders for detail and search-related suites. Large files such as `frontend/src/views/__tests__/EvidenceView.test.tsx`, `SourceDetailView.test.tsx`, and related API-client tests repeat the same mock entity/source/relation payloads and render helpers, making tests verbose and harder to update when contracts change.
- Split `backend/app/api/document_extraction.py` into cohesive modules by workflow: document extraction endpoints, URL extraction endpoints, PubMed bulk search/import endpoints, smart discovery endpoints, and shared request/response schemas. The current 1500+ line file is a transport-layer monolith.
- Break `backend/app/services/search_service.py` into smaller modules: per-domain search adapters (`entity_search`, `source_search`, `relation_search`), shared ranking/snippet helpers, and a thin coordinator service. The current service bundles three search implementations plus aggregation/pagination.
- Decompose `backend/app/services/user_service.py` into account lifecycle modules (registration/profile updates), credential flows (authenticate/change/reset password), token lifecycle, and email verification flows. The current service is a single auth/account monolith.
- Split `backend/app/services/extraction_review_service.py` into staging, review decisions, and materialization modules/use-cases. The current file mixes state transitions, persistence access, and final entity/relation creation behavior.
- Separate `backend/app/services/inference_service.py` into inference query/loading, scoring/aggregation math, scope matching/cache conversion, and cache persistence modules. The current file couples computation logic with cache and relation-loading concerns.
- Break `frontend/src/views/SmartSourceDiscoveryView.tsx` into smaller modules: entity-loading hook, search-form component, results-table component, selection/import state hook, and route/query-param sync helper. The current view is a page-sized workflow module.
- Split `frontend/src/views/SourceDetailView.tsx` into source metadata, relations panel, extraction actions, extraction preview/save state, and destructive-action dialog modules. The current component owns too many subdomains for one view file.
- Decompose `frontend/src/views/SynthesisView.tsx`, `DisagreementsView.tsx`, and `EvidenceView.tsx` into page containers plus dedicated statistics/summary/trace sections. These views currently mix fetch orchestration with large domain-specific rendering blocks.
- Extract shared query-string/filter builders from frontend API modules (`entities.ts`, `sources.ts`, similar clients) into reusable transport helpers so each API module only defines domain contracts and endpoint paths.
- Split oversized test files into behavior-focused suites. Files like `EvidenceView.test.tsx`, `SynthesisView.test.tsx`, and filter component test bundles are becoming monolithic enough that failures are hard to localize and maintenance cost is rising.
- Decouple service-to-service construction in the backend. Services such as `ExplanationService` directly instantiate `InferenceService` and `SourceService`, which binds them to concrete implementations and shared DB/session assumptions; introduce constructor-injected interfaces or application-layer collaborators.
- Reduce controller coupling to concrete workflow components. API modules currently import and assemble concrete orchestrators/factories (`BatchExtractionOrchestrator`, `MetadataExtractorFactory`, `PubMedFetcher`, `UrlFetcher`, bulk/linking services) inline, making transport code dependent on implementation details instead of stable use-case interfaces.
- Separate repositories/query adapters from domain services that still depend on ORM models directly. `SearchService`, export services, derived-property services, and parts of extraction/review logic know concrete table structure and SQL expression details, coupling domain behavior to persistence schema changes.
- Extract plugin-style interfaces for external knowledge adapters. Metadata extraction, PubMed/PMC fetching, URL fetching, and LLM batch extraction are wired together through direct imports and instantiation chains, making it hard to replace one provider without touching multiple modules.
- Remove frontend coupling between page components and many low-level API clients/utilities. Views like `SourceDetailView` depend directly on source, relation, extraction APIs, notification transport, cache invalidation helpers, and router state; introduce page-level controller hooks/services so components consume one coherent interface.
- Consolidate frontend filter/query serialization behind shared client utilities. API modules such as `entities.ts`, `sources.ts`, and similar duplicate `URLSearchParams` construction rules, coupling each domain client to transport formatting details and making filter changes fan out across files.
- Introduce stable module boundaries for inference/explanation read models. Frontend views and backend services both depend on raw `InferenceRead`/relation structures and compute secondary summaries themselves, creating coupling to internal response shape rather than dedicated view-model or query objects.
- Break import-time coupling caused by inline/lazy imports used to avoid cycles. Files such as `document_extraction.py`, `typedb_export_service.py`, `extraction_service.py`, and other modules with nested imports should be reorganized around explicit interfaces to remove hidden dependency edges.
- Move business rules for source classification out of infrastructure-facing services. `SourceService` still embeds domain/category policy (`DOMAIN_KEYWORDS`, graph-role thresholds) beside SQL query construction; extract those rules into domain policy modules that repositories/query builders can consume.
- Isolate search relevance policy from persistence details. `SearchService` currently mixes ranking heuristics, snippet-generation rules, UUID/filter normalization, and raw ORM query construction in the same methods; split domain ranking/snippet policy from repository/query execution.
- Create extraction application services that own workflow decisions. `api/document_extraction.py` currently blends HTTP concerns with business orchestration such as source eligibility checks, query construction from entities, import decision rules, and multi-step extraction/linking/save flows.
- Remove test-only fallback behavior from production transport modules. Deterministic PubMed fallback data and compatibility facades inside `api/document_extraction.py` mix testing/integration infrastructure concerns into runtime business endpoints; move them behind dedicated adapters or test-only wiring.
- Introduce frontend page-controller hooks to separate domain view models from transport/UI details. Views like `SourceDetailView`, `SynthesisView`, and `ExplanationView` currently combine API calls, error parsing/notifications, workflow decisions, and domain-derived summaries directly inside React components.
- Centralize computed presentation logic that mirrors backend domain concepts. Frontend views currently derive consensus bands, contradiction counts, confidence summaries, and extraction-state decisions locally, which mixes domain interpretation with rendering and risks drift from backend rules.
- Define a dedicated application boundary for cross-service workflows that currently span infrastructure objects directly. Multi-step flows often instantiate services, fetch ORM-backed data, and apply business branching in the same function rather than through a distinct use-case layer.
- Introduce an application-layer workflow for extraction/discovery (`document_extraction`, PubMed import, smart discovery). Current API controllers orchestrate multiple services, run DB queries, define compatibility facades, and own validation/branching that should live outside the HTTP layer.
- Enforce the documented repository boundary in backend services. `SourceService`, `SearchService`, and similar services still build large ORM queries directly against models instead of delegating query construction/data access to repositories or dedicated query objects.
- Split search into modular query components. `SearchService` currently duplicates the same lifecycle per type (build query, compute relevance, count, paginate, map results); create shared search abstractions plus per-domain adapters to reduce coupling and keep ranking logic testable in isolation.
- Replace repeated FastAPI query-parameter wiring with request/filter DTO dependencies. Endpoints like `api/sources.py` and `api/search.py` duplicate filter definitions already modeled in schemas, which drifts validation/docs and spreads transport concerns across controllers.
- Extract frontend page-controller hooks for detail/workflow views. Views such as `SourceDetailView`, `SynthesisView`, `ExplanationView`, and related detail screens still combine routing, API orchestration, derived-state computation, and presentation in one component instead of a container hook + presentational component split.
- Consolidate frontend async resource patterns into shared abstractions. Detail pages repeat the same loading/error/refetch/partial-failure mechanics with slight variations, which creates inconsistent behavior and couples each view to raw API coordination.
- Separate ingestion/test-support concerns from production domain flows. `api/document_extraction.py` mixes real workflow code with E2E fallback behavior and compatibility scaffolding, which makes the module responsible for runtime orchestration, testing accommodations, and transport details at once.
- Introduce dedicated domain modules for reusable classification policies. Heuristics like source-domain keywords, graph-role classification rules, and search ranking priorities are scattered inside services/controllers instead of living in named policy modules with a single source of truth.
- Split `backend/app/api/document_extraction.py` monolithic endpoints (`smart_discovery`, `extract_from_url`, related PubMed flows) into validation/helpers/service orchestration steps; remove inline imports and ad-hoc traceback printing
- Refactor `backend/app/services/search_service.py` long `_search_*` methods into shared query/result-mapping helpers with clearer names for intermediate result sets
- Re-run backend error-handler tests once the local backend test environment includes `pytest_asyncio`
- Re-run the targeted Playwright explanation error-path spec once the E2E test API on `127.0.0.1:8001` is available
