# Caddyfile (development)
:80 {
	# Preserve /api because FastAPI routers are mounted under that prefix.
	handle /api/* {
		reverse_proxy api:8000
	}

	# Everything else -> React dev server
	handle {
		reverse_proxy web:3000
	}
}
