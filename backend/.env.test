# Test Environment Configuration
ENV=test
PROJECT_NAME=hyphagraph-test

# Database - using SQLite for testing
DATABASE_URL=sqlite+aiosqlite:///./test.db

# Security
SECRET_KEY=test-secret-key-for-testing-only
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# Admin User
ADMIN_EMAIL=test-admin@example.invalid
ADMIN_PASSWORD=replace-in-runtime

# Rate Limiting
RATE_LIMIT_ENABLED=False

# Email (disabled for testing)
EMAIL_ENABLED=False
EMAIL_FROM=tests@example.invalid
EMAIL_FROM_NAME=HyphaGraph Test

# Frontend URL
FRONTEND_URL=http://localhost:3000

# Email Verification
EMAIL_VERIFICATION_REQUIRED=False

# LLM Configuration
# OPENAI_API_KEY should be set in .env (not committed) or as environment variable
OPENAI_MODEL=gpt-5.4
OPENAI_TEMPERATURE=0.3

# Flags
STRICT_VALIDATION=True
SQL_DEBUG=False
